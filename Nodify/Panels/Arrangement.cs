namespace Nodify
{
    public enum Arrangement
    {
        Stack,
        Grid,
        AutoGrid,
        Wrap, 
        Uniform, 
        Custom, 
        UniformStack, 
        Dock,
        TreeMap,
        Tree,
        UniformRow,
        Standard
    }
}